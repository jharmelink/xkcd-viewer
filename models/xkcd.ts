export interface XkcdResponse{
    "data": {
        number: number,
        url: string,
        title: string,
        description: string,
        year: number,
        month: number,
        day: number
    }
}
